# 📄 PDF 合併神器 (Serverless PDF Merger)

### 🔴 [點擊這裡立即使用 (線上版)](https://stty0219.github.io/pdf-merger/)

這是一個完全免費、開源，且**極度重視隱私**的 PDF 合併工具。
與市面上的線上轉檔網站不同，本工具基於 **PyScript** 技術，所有運算皆在您的**瀏覽器本機**執行。

**您的檔案永遠不會被上傳到伺服器，確保 100% 的資料安全。**

---

## 🌟 為什麼選擇這個工具？

1.  **🔒 零隱私風險**
    * 您合併的是論文草稿、銀行帳單或合約嗎？不要上傳到來路不明的網站！
    * 本工具不依賴後端伺服器，網路斷線也能跑，資料絕不出家門。

2.  **🐍 Python 原生引擎**
    * 核心使用 Python 強大的 `pypdf` 套件，確保合併後的檔案結構精確、Metadata 資訊完整。

3.  **🖱️ 直覺的拖拉排序**
    * 支援拖曳 (Drag & Drop) 調整檔案順序，想把第二章移到第一章前面？拉過去就好。

4.  **⚡ 輕量快速**
    * 無需安裝 Python，無需下載軟體，打開網頁即用。

---

## 📖 使用教學

1.  點擊上方的**線上版連結**。
2.  點擊虛線區域選擇 PDF 檔案（或直接將檔案拖入）。
3.  **調整順序**：按住檔案左側的 `☰` 圖示上下拖曳。
4.  點擊 **「🚀 開始合併」**。
5.  等待數秒，點擊出現的連結下載合併後的檔案。

---

## 🛠️ 技術架構 (Technical Details)

本專案展示了如何將 Python 強大的資料處理能力帶入前端網頁。

* **Runtime**: [PyScript](https://pyscript.net/) (WebAssembly) - 讓瀏覽器能讀懂 Python。
* **Core Library**: `pypdf` - 處理 PDF 串流與合併邏輯。
* **Interaction**: `SortableJS` - 處理流暢的拖曳排序體驗。
* **Metadata**: 自動寫入正確的 Producer 標籤 (`Python 3.x + pypdf`)。

---

## 💻 本地開發 (Development)

如果您想在自己的電腦上修改此專案：

1.  Clone 此倉庫：
    ```bash
    git clone [https://github.com/](https://github.com/)[您的帳號]/pdf-merger.git
    ```
2.  進入目錄並啟動本地伺服器 (因 CORS 安全限制，無法直接開啟 HTML)：
    ```bash
    cd pdf-merger
    python -m http.server
    ```
3.  瀏覽器開啟 `http://localhost:8000`

---

> Created with ❤️ by [您的名字/帳號]
